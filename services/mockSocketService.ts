
// This file is no longer used as the application has switched to a CSV-based DSS model.
// Keeping a stub export to prevent build errors if imports remain.
export const socketService = {
    connect: () => {},
    disconnect: () => {},
    login: async () => false
};
